# =============================================================================
# NEXA ACADEMY - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it contains sensitive credentials
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Client-side)
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard: https://app.supabase.com
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# -----------------------------------------------------------------------------
# Cloudflare Stream Configuration
# -----------------------------------------------------------------------------
# Client-side: Get from Cloudflare Stream dashboard
VITE_CLOUDFLARE_ACCOUNT_HASH=your_cloudflare_account_hash

# Server-side (Edge Functions only - no VITE_ prefix):
# Create a Cloudflare API token with Stream permissions
# https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Get from Stripe dashboard: https://dashboard.stripe.com/apikeys
# Use TEST keys for development, LIVE keys for production

# Client-side publishable key (safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Server-side secret key (NEVER expose to client)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Webhook signing secret (from Stripe webhook configuration)
# https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# -----------------------------------------------------------------------------
# Stripe Subscription Price IDs
# -----------------------------------------------------------------------------
# Platform-level subscriptions (fixed prices)
# Create these in Stripe dashboard: Products > Add Product
# See STRIPE_SUBSCRIPTION_SETUP.md for detailed setup instructions

# Monthly platform subscription (e.g., €29.99/month)
VITE_STRIPE_PLATFORM_MONTHLY_PRICE_ID=price_monthly_subscription_id

# Yearly platform subscription (e.g., €299.99/year)
VITE_STRIPE_PLATFORM_YEARLY_PRICE_ID=price_yearly_subscription_id

# NOTE: Professor subscriptions use dynamic pricing - no Price IDs needed
# They are created programmatically based on professor.subscription_price

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
#
# 1. SECURITY:
#    - Never commit .env file to git
#    - Rotate all credentials before production deployment
#    - Use different keys for development and production
#
# 2. EDGE FUNCTIONS:
#    - Supabase automatically injects these variables (no manual config):
#      * SUPABASE_URL
#      * SUPABASE_ANON_KEY
#      * SUPABASE_SERVICE_ROLE_KEY
#    - Only CLOUDFLARE_* and STRIPE_* need to be manually configured
#
# 3. PRODUCTION CHECKLIST:
#    - [ ] Replace all test keys with production keys
#    - [ ] Configure Stripe webhook endpoint in dashboard
#    - [ ] Set up Cloudflare API token with minimal permissions
#    - [ ] Verify all environment variables in hosting platform
#    - [ ] Test payment flow end-to-end
#
# =============================================================================
